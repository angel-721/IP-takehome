Example use:

```
python part2.py --query backpacks -isH False -i "I work from home"
```

So -q flag is the query and -i can be a string of your interest if you want a brief summary on how the product might be useful towards your interests. It defaults to being a student
